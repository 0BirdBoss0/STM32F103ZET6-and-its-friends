#ifndef __TIMER_H
#define __TIMER_H

#define RECORD_WAITING_TIME 15000 // unit: milliseconds

void Timer2_Init(void);
void Timer3_Init(int period);
void Timer4_Init(int period);
void Timer5_Init(int period);

#endif
